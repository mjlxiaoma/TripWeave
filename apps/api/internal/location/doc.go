// Package location provides map-backed place lookup and day routing: a thin
// Amap (高德) Web Service client, locations-table persistence and the HTTP
// endpoints used by the planner map panel. The Web Service key stays
// server-side; the frontend only holds the separate JS API key.
package location
