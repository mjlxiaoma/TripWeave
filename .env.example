# TripWeave 环境变量说明
#
# 各应用使用自己的 .env 文件（均已被 .gitignore 忽略，不会提交）：
#   apps/api/.env        —— 后端配置（数据库、Redis、JWT、DeepSeek、高德 Web 服务、SMTP）
#   apps/web/.env        —— 前端配置（高德 JS API Key + 安全密钥）
#
# 模板见同目录下对应的 .env.example 文件。
